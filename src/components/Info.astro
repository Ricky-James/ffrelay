<section id="info" class="section-pad">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8 max-w-4xl">
    <p class="eyebrow">About the event</p>
    <h2 class="section-heading">What is the Final Fantasy Relay?</h2>

    <div class="info-body">
      <p>
        The Final Fantasy Series Relay Race is a community celebration of Final Fantasy Speedrunning. The race runs non-stop across 4 days, while 2 or 3 teams compete to finish a set of games from the Final Fantasy franchise in the shortest combined time.
      </p><p>
        The event is run as a relay race, which means when a runner completes  game, the runner for the next game on the same team starts immediately. The team that finishes all of the games first is the winner!
      </p>
    </div>
  </div>
</section>

<style>
  .section-pad {
    padding: 6rem 0;
  }
  .eyebrow {
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--text-dim);
    margin-bottom: 1rem;
  }
  .section-heading {
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 800;
    letter-spacing: -0.02em;
    color: var(--text);
    margin-bottom: 2rem;
  }
  .info-body {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
    font-size: 1.05rem;
    line-height: 1.7;
    color: var(--text-muted);
  }
</style>
